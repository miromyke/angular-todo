{
  "name": "angular-todos",
  "devDependencies": {
    "gulp": "^3.8.11",
    "gulp-concat": "^2.5.2",
    "gulp-less": "^3.0.3"
  },
  "scripts": {
    "start": "gulp watch",
    "postinstall": "bower i && gulp && node tools/inject_hooks.js"
  }
}
